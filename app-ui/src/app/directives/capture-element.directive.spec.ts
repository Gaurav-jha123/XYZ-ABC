import { CaptureElementDirective } from './capture-element.directive';

describe('CaptureElementDirective', () => {
  it('should create an instance', () => {
    const directive = new CaptureElementDirective();
    expect(directive).toBeTruthy();
  });
});
